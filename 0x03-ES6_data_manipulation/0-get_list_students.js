export default function getListStudents() {
  const est1 = { id: 1, firstNme: 'Guillaume', location: 'San Francisco' };
  const est2 = { id: 2, firstNme: 'James', location: 'Columbia' };
  const est3 = { id: 5, firstNme: 'Serena', location: 'San Francisco' };

  return [est1, est2, est3];
}
